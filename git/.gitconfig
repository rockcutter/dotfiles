[user]
	name = rockcutter
	email = rockcutter414@gmail.com
[alias]
	sw = switch
	swf = "!f () { git switch $(git --no-pager branch --sort=-committerdate | fzf); };f"
	s = status
	ss = status -s
	s = switch
	cm = commit
	cmm = commit -m
	b = branch
	cdiff = diff --cached
	# addf = "!f () { git add $(git status -s | awk '{print $2}' | fzf -m --preview 'git diff -- {1}' | sed -e 's/\n/ /g'); git status -s; };f"

[core]
	editor = nvim
[push]
	autoSetupRemote = true
